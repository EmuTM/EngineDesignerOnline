import { CustomMenuDirective } from './custom-menu.directive';

describe('CustomMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMenuDirective(null);
    expect(directive).toBeTruthy();
  });
});
